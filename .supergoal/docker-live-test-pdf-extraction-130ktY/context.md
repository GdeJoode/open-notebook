# Environment context (hard-won — read before executing)

- **OS/shell:** Windows 11. Two shells: Git Bash (Bash tool) runs Windows binaries; WSL (Ubuntu) runs Linux. The repo is on NTFS at `E:\repos\private\open-notebook` = `/mnt/e/repos/private/open-notebook` in WSL.
- **Python env = WSL venv.** Run python/pytest/ruff via `wsl bash -lc '… /mnt/e/repos/private/open-notebook/.venv/bin/python …'`. The `.venv` was just rebuilt for Linux via `uv sync --all-packages --all-extras` (pytest is in member extras, not groups). A Windows-side `uv`/`pip` will clobber `.venv` back to `Scripts/` (Windows) — keep venv builds on the WSL side only.
- **Docker = Docker Desktop** (engine 28.5.1), WSL2 backend. GPU works (RTX 5070 Ti, nvidia runtime). `docker` works from Git Bash.
- **`wsl bash -lc '...'` quoting:** backticks inside the single-quoted string trigger command substitution — avoid backticks; use `git commit -F <file>` for messages and the Write tool for files with backticks.
- **rev-parse glitch:** `git rev-parse`/`branch --show-current` chained right after a `git checkout` in one `wsl bash -lc` can echo STALE values. Verify branch state in a FRESH wsl call. Authoritative signal for a checkout/merge is the git command's own message + a fresh re-query.
- **Ollama** runs on host at `http://localhost:11434` (in containers: `http://host.docker.internal:11434`). Models present: `nomic-embed-text`, `mxbai-embed-large`, `llama3.1:8b-instruct-q4_0`, `llama3.1:8b`, `llama3.2:3b`, `qwen2.5:14b(-instruct-q5_K_M)`, `qwen3.5:9b`, `qwen3.5:35b-a3b`, `granite3.2-vision`. All models the compose services reference are pulled.
- **Test PDFs:** `tests/pdfs/` — `Economics_without_equilibrium(2).pdf`, `J of Common Market Studies - 2025 - Ali - …Urban Rural Classification.pdf`, `the_centrifugal_state…decentr.pdf`.
- **Docker stack (`docker-compose.yml`):** `surrealdb` (8000, UP), `open_notebook` (8502 frontend + 5055 API; depends_on surrealdb+docling), `docling` (8100, GPU), `mineru` (8104, GPU), `summarization` (8101), `extraction` (8103), `whisperx` (8102, GPU). `open_notebook` env_file = `docker.env`. App runs DB migrations on startup (lifespan) — this is where migration 49 (I.F) actually applies.
- **open_notebook image was 2 weeks old** (pre-Track-I). REBUILD in progress (bg task `bs69d1v42`, `docker compose build --progress=plain open_notebook`). NOTE: build context is 1GB+ because `.dockerignore` doesn't exclude `.claude/` (leftover agent worktrees with their own venvs). Build still completes; a `.dockerignore` `.claude/` fix is a noted follow-up, not required for this run.
- **KG entity/relation extraction is a SEPARATE step** (`apps/app-main/src/app_main/api/routers/knowledge_graph.py`), NOT auto-run by `process_source` (which does parse → chunk → structure-graph I.F). Ingest first, then trigger KG extraction.
- **Track I merged to main** (A,H1,C,B,D1-4,E,F); H3 out of scope; G+H2 remain. `main` = `1a07c91` at run start.
