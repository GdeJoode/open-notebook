test(e2e): result-tabs spec — tab switching, markdown sanitization, lazy images (I.D-4)

Route-mocked Playwright spec covering the three ACs: all five tabs render
without console errors; the Markdown tab strips an injected <script> +
onerror <img> (sanitization); the Images tab mounts at most 6 lazy <img>
per page with pagination. Validated via --list (headless run unavailable
in this environment).

Co-Authored-By: Claude Opus 4.8 (1M context) <noreply@anthropic.com>
