-- ============================================================================
-- Migration 62 DOWN: no-op.
--
-- Migration 62 is a self-healing schema/data repair: it deletes null-endpoint
-- junk rows and re-asserts the canonical `relation` TYPE RELATION definition
-- (migration 39). There is no faithful inverse —
--   * the deleted rows were malformed legacy junk (null in/out), not real data
--     worth restoring; and
--   * re-defining the table back to NORMAL would re-break `RELATE` and is
--     exactly the drift 62 exists to fix.
-- Down intentionally does nothing; mirrors migration 61's down convention. The
-- canonical relation definition lives in migration 39 and is re-asserted by
-- both 39 and 62 on any forward run.
-- ============================================================================
