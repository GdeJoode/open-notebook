# Phase A.3 — Integration + Documentation (Track A: MinerU) — Sprint Plan

> Generated by track-planner agent on 2026-06-04
> Status: draft (awaiting human approval)
> Scope: ONE PR, ~1 day. Branch: `track/a-mineru-integration` (already created off `main`; A.0/A.1a/A.1b/A.1c/A.2 all merged).

## 1. Context

- Parent: `docs/tracks/A-mineru/plan.md` §Phase A.3.
- Prereqs done: A.0 (Playwright), A.1a (MinerU service + HTTP client), A.1b (`parser_engine` dispatcher), A.1c (confidence fallback + `Source.metadata`), A.2 (Settings dropdown + slider + health chip + `ParserEngineBadge` + per-source override + 4 per-feature Playwright specs).
- Goal: validate the end-to-end auto-fallback flow against real PDFs, confirm or revise the default 0.95 confidence threshold, document the feature for users and operators, and close the track.
- A.3 is **intentionally smaller than A.2**: this is a docs + tuning + integration-test PR. No new product code is required to ship A.3; small follow-up tweaks (e.g. revised default threshold) are allowed inside the same PR if tuning surfaces them.

**Backend / frontend state confirmed by code survey (2026-06-04)**:

- `apps/app-main/src/app_main/services/parsing/confidence.py` — `score_docling_extraction()` is a pure function importable from `apps/app-main/src/app_main/services/parsing/__init__.py` (re-exports `DEFAULT_THRESHOLD`, `SIGNAL_WEIGHTS`, `DoclingConfidenceScore`). The threshold-tuning script in §2 calls it directly with no service plumbing.
- `apps/app-main/src/app_main/services/parsing/auto_fallback.py` — `extract_with_auto_fallback()` is async, accepts injected `docling_client` and `mineru_client`. The tuning script can run docling-only (skip MinerU) by passing a `MagicMock` whose `process` is never awaited, or by short-circuiting after the score is computed.
- `frontend/e2e/track-a/_track-a-helpers.ts` already exports `mockDashboardChrome` and `DEFAULT_SETTINGS`. The new integration spec(s) reuse both.
- `docs/development/architecture.md` exists at **497 lines**; the relevant section is `### 5. Background Processing (`commands/`)` at line 139 — the doc predates the M0 refactor and does NOT currently mention `services/docling`, `services/mineru`, the dispatcher, or `Source.metadata`. A.3 adds a small dedicated subsection (≤ 1 paragraph + a short component list).
- `README.md` has a "For Contributors" section pointing at `frontend/e2e/README.md`. The "Parser engines" subsection lands under `### 🛠️ Full Installation` (line 266) **or** as a new short subsection under `### Advanced Features` (line 316) — see §5 open question.
- `docs/troubleshooting/` exists with `common-issues.md`, `debugging.md`, `faq.md`, `index.md`, `quick-fixes.md`. A new file `parser-engines.md` is **additive** — no existing file is modified beyond a one-line pointer from `index.md`.

**Fixtures available** (survey of `find -name "*.pdf"`):

- `tests/fixtures/` — **does NOT exist** (no canonical Python test-fixture directory)
- `apps/app-main/tests/fixtures/` — **does NOT exist**
- `docling_input/` (8 real PDFs, gitignored runtime input): mix of Dutch policy reports (`Convenant_*.pdf`, ~270-310 KB), fiscal-equalisation academic PDF (`fiscal_equalisation.pdf`, 672 KB), large clean PDF (`Van-meer-waarde-Nationaal-Netwerk-Brede-Welvaart-2025.pdf`, 4.1 MB), large academic (`Bennett_1983.pdf`, 30 MB), small academic test (`Bennett_test.pdf`, 3.3 MB).
- `data/uploads/` — populated user uploads, **NOT redistributable** (likely user-private documents). Not eligible as committed fixtures.

**Decision**: do **NOT** commit large PDFs into the repo. The threshold-tuning workflow runs against developer-local `docling_input/` files (already gitignored), with the **report** committed in `docs/tracks/A-mineru/threshold-tuning.md`. For CI-reproducibility, a tiny set (≤200 KB each) of **synthetic** PDFs covering the four categories (clean text, OCR-heavy, table-heavy, mixed) is generated into `apps/app-main/tests/fixtures/` and used by the integration Playwright spec via route-mocking (so MinerU and SurrealDB never need to actually parse them). See §3 file list.

**Conflicts / coordination**:

- No other track currently touches the files in §3. The track is closing; nothing further is stacked on it.
- Branch is `track/a-mineru-integration` (off latest `main` which already has A.0-A.2 merged). No rebase risk.

---

## 2. Goals (this phase)

1. **Threshold tuning** — survey ≥ 3 real PDFs through `score_docling_extraction()`, record signals + decision + actual quality, decide whether to keep 0.95 default.
2. **End-to-end Playwright spec** — one spec covering the full docling → score → auto-fallback → MinerU → metadata-persistence → `ParserEngineBadge` flow, CI-runnable via route mocks.
3. **Documentation** — README subsection, ARCHITECTURE.md update, new troubleshooting page, status.md final entry, retro.
4. **No regressions** — full pytest + frontend lint + tsc + Playwright suites still green.

Explicit **non-goals** for A.3:

- New product code (only `threshold-tuning.py` helper script + Playwright spec; no API change, no UI change).
- Live SurrealDB / MinerU container runs in CI (still mocked, per A.1c "Testing gap" note).
- Threshold change *without* a documented justification in `threshold-tuning.md`.

---

## 3. Files to create

| Path | Purpose |
|---|---|
| `docs/tracks/A-mineru/threshold-tuning.md` | Final tuning report — fixture table, raw scores, decision + rationale. |
| `apps/app-main/scripts/score_pdf_corpus.py` | Standalone CLI: `uv run --project apps/app-main python -m app_main.scripts.score_pdf_corpus <pdf-or-dir>` → prints a Markdown row per PDF (filename, page count, overall, per-signal, decision @ 0.95). No API plumbing; instantiates `DoclingHttpClient` (or in-process `IngestionWorkflow` if `USE_DOCLING_SERVICE=false`) directly. Side-effect-free except stdout. ~80 LOC. |
| `apps/app-main/tests/test_score_pdf_corpus.py` | Smoke test: instantiates the CLI's helper functions with a synthetic `IngestionResult` and asserts the Markdown row format is parseable (one row per fixture, correct column count). ~30 LOC. |
| `frontend/e2e/track-a/parser-engine-integration.spec.ts` | The single E2E spec — see §4 for what it asserts. ~150 LOC. |
| `docs/troubleshooting/parser-engines.md` | New troubleshooting page; ≥3 scenarios (see §5). |
| `docs/tracks/A-mineru/RETRO.md` | Final track retrospective (what worked / what hurt / recommendations for B-G). |
| `apps/app-main/tests/fixtures/README.md` | Marker explaining what's in this directory and why fixtures are small. |
| `apps/app-main/tests/fixtures/synthetic_clean_text.pdf` | Tiny (≤30 KB) reproducible synthetic PDF — single-page lipsum-style text generated via `reportlab`. Used by the Playwright spec route-mock and as a reference fixture for the tuning report. |
| `apps/app-main/tests/fixtures/synthetic_table_heavy.pdf` | Tiny (≤50 KB) synthetic PDF with a 4-column / 8-row table, generated via `reportlab`. |
| `apps/app-main/tests/fixtures/synthetic_image_only.pdf` | Tiny (≤100 KB) synthetic PDF embedding a single PNG screenshot (the project logo from `docs/assets/hero.svg` rendered to PNG once) — stands in for the OCR-heavy / scanned-doc category. |
| `apps/app-main/tests/fixtures/generate.py` | Script that regenerates the three synthetic PDFs from scratch — committed so reviewers can reproduce. Uses only `reportlab` (already a transitive dep of `docling`; verify with `uv tree`). If `reportlab` is not present, fall back to ChatGPT-generated PDFs committed as-is; document in this script's header. |

**Note on the synthetic fixtures**: they are NOT meant to give realistic confidence scores (they're too small to reflect a 20-page paper). Their sole purpose is: (a) anchor the Playwright spec with deterministic file paths, and (b) give the tuning report a reproducible "baseline" row. The real tuning happens against the developer-local `docling_input/` PDFs, which the report names but does not commit.

## 4. Files to modify

| Path | Change |
|---|---|
| `README.md` | Add a 12-15-line subsection "Parser engines" under `### Advanced Features` (line 316) — see §6 below for exact copy. |
| `docs/development/architecture.md` | Add ~1 paragraph + 4-line bullet list to the **§2 Domain Layer** subsection (line 66) OR a new subsection "Source extraction pipeline" before §3 — whichever the reviewer prefers (§5 open question). Mentions: `services/docling/` + `services/mineru/` HTTP services, the dispatcher (`apps/app-main/src/app_main/services/parsing/engine_dispatcher.py`), auto-fallback (`auto_fallback.py`), and the `Source.metadata` provenance bag. |
| `docs/troubleshooting/index.md` | Add a one-line pointer in the existing index to `parser-engines.md`. |
| `docs/tracks/A-mineru/status.md` | Final entry: `## Phase A.3 — IMPLEMENTED (2026-06-XX)` block listing tuning outcome, docs files, retro, "Track A complete" marker. |
| `docs/FEATURE_ROADMAP.md` | Track A row → ✅ done; brief note pointing to RETRO + threshold-tuning report. |

## 5. The integration Playwright spec (`parser-engine-integration.spec.ts`)

This is the single spec the prompt requires. It exercises the **complete** flow with route mocks so it runs on CI without MinerU or a real backend:

```
1. mockDashboardChrome(page)                  ← reuse A.2 helper
2. Mock GET /api/settings → parser_engine:'auto', docling_min_confidence:0.95
3. Mock GET /api/health/mineru → healthy:true
4. Mock POST /api/sources (upload):
     - status 200, returns source.id='src_test_e2e'
     - simulates backend writing Source.metadata with
       parser_engine_used='mineru', extraction_confidence=0.62,
       extraction_fallback_triggered=true,
       extraction_confidence_signals={ ocr_confidence:0.4, … }
5. Mock GET /api/sources/src_test_e2e → returns the above source
6. Drive UI:
     - Navigate to /settings, confirm parser_engine dropdown shows 'auto'
       and slider reads 0.95 (verifies persistence from A.2)
     - Navigate to /notebooks/{some_id}, click "Upload source", select
       synthetic_clean_text.pdf, submit
     - Wait for source detail to load (page.waitFor on the badge selector)
     - Assert <ParserEngineBadge /> shows amber "MinerU (auto-fallback, conf 0.62)"
     - Hover the badge, assert tooltip lists the per-signal breakdown
     - Click "Reprocess", change "Parser engine for this run" → 'docling',
       click Reprocess Document
     - Intercept POST /sources/src_test_e2e/reprocess, assert body has
       parser_engine: 'docling' (verifies per-source override end-to-end)
```

**CI safety**: spec is **fully mocked** — no MinerU container, no real SurrealDB, no real docling. `test.skip(!process.env.MINERU_AVAILABLE, ...)` is **not** used because no part of the spec touches an actual service. A second skip-on-CI **smoke** variant is acceptable but **out of scope for A.3** — that would be follow-up work after a real MinerU deployment.

**Assertion bar**: at minimum the spec verifies criteria 3 + 8 from the parent plan (full flow visible, metadata reflected in badge) and per-source override. Other A.2 specs already cover settings round-trip, badge states, health chip; this spec uniquely covers the **integration sequence**.

## 6. Threshold tuning workflow

**Inputs**:

- Developer-local `docling_input/` PDFs (8 already on disk in this workspace; reviewer can supply their own corpus if reproducing):
  - **Clean text**: `Convenant_Zuid-Oost-Drenthe-II.pdf` (Dutch policy doc, native PDF, mostly text)
  - **Table-heavy**: `Bijlage 3. Lopende trajecten Achterhoek NPVR.pdf` (small layout-heavy doc)
  - **Mixed content**: `fiscal_equalisation.pdf` (academic, formulas + tables + figures)
  - **Long-form clean**: `Van-meer-waarde-Nationaal-Netwerk-Brede-Welvaart-2025.pdf` (4 MB, 50+ pages)
  - **OCR-needed proxy**: `Bennett_test.pdf` (small academic with at least one scanned page — verify before running)
- Optionally, the three committed synthetic fixtures (§3) — included in the report as a baseline row but not relied upon for the tuning decision (they're too small to drive realistic scoring).

**Procedure** (documented in `threshold-tuning.md` reproducibly):

```bash
# 1. Bring up docling service only (no MinerU needed for the score itself)
docker compose up -d docling

# 2. Score each PDF
cd apps/app-main
uv run python -m app_main.scripts.score_pdf_corpus ../../docling_input/

# 3. Output is a Markdown table; paste into threshold-tuning.md
```

**Report shape** (`threshold-tuning.md`):

```markdown
# Track A — Default-threshold tuning

> Run date: 2026-06-XX
> Default threshold under test: 0.95 (from `DEFAULT_THRESHOLD` in confidence.py)

## Corpus

| # | Fixture | Type | Pages | Notes |
|---|---|---|---|---|
| 1 | Convenant_Zuid-Oost-Drenthe-II.pdf | Clean text | … | … |
| 2 | Bijlage 3. Lopende trajecten Achterhoek NPVR.pdf | Table-heavy | … | … |
| 3 | fiscal_equalisation.pdf | Mixed | … | … |
| 4 | Van-meer-waarde…2025.pdf | Long-form clean | … | … |
| 5 | Bennett_test.pdf | OCR / scanned proxy | … | … |

## Scores (@ default 0.95)

| Fixture | overall | OCR | density | heading | table | image | unknown | decision @0.95 | manual quality call |
|---|---|---|---|---|---|---|---|---|---|
| Convenant_…II | … | … | … | … | … | … | … | accept | docling OK |
| Bijlage 3 | … | … | … | … | … | … | … | … | … |
| fiscal_equalisation | … | … | … | … | … | … | … | … | … |
| Van-meer-waarde | … | … | … | … | … | … | … | accept | docling OK |
| Bennett_test | … | … | … | … | … | … | … | fallback | mineru helps? |

## Decision

(One of the three branches below — chosen based on the table above.)

### Branch A — Keep 0.95 (most likely)
Of the N fixtures, K trigger fallback at 0.95. Manual inspection
confirms the K fallback-triggered docs are genuine candidates
(OCR-heavy / table-heavy with low parsing success). The non-fallback
docs (N-K) read cleanly under docling. **Conclusion: keep 0.95 default.**

### Branch B — Lower to 0.85-0.90 (if 0.95 over-triggers)
If ≥ 50 % of *clean* docs score below 0.95 (e.g. native PDFs with
no images scoring 0.88-0.92 due to heading saturation never reaching
2/page), the default is too aggressive. Sweep at 0.85 / 0.90 / 0.92,
pick the value where ≤ 20 % of clean-corpus docs trigger fallback.
**Change**: bump `DEFAULT_THRESHOLD` in
`apps/app-main/src/app_main/services/parsing/confidence.py`, and the
`docling_min_confidence` default in
`packages/shared/src/shared/models/settings.py` + the slider default
in `SettingsForm.tsx`. All three must move together.

### Branch C — Raise to 0.97 (if 0.95 under-triggers)
If known-bad docs (OCR'd scans, formula-heavy academics) are
scoring above 0.95, the bar is too low. Inspect the signal weights;
raising the threshold uniformly is simpler than re-weighting.

## Reproducibility

Run `uv run --project apps/app-main python -m app_main.scripts.score_pdf_corpus ./docling_input/`.
Output rows above were captured on YYYY-MM-DD against docling image
sha256:… (paste from `docker compose images docling`).
```

The report **must** end with a single bold sentence: "Decision: keep default at 0.95" OR "Decision: change default to X — see PR diff in confidence.py:11, settings.py:42, SettingsForm.tsx:65".

## 7. The README "Parser engines" subsection

Exact copy (drop into README.md under `### Advanced Features`):

```markdown
### Parser engines

Open Notebook ships with four document-parser engines, switchable per-document or globally in Settings → Content Processing:

- **Docling** (default) — battle-tested general parser. Works well for most native PDFs, Word docs, web pages, and Markdown. GPU-accelerated when available.
- **MinerU** — alternative GPU parser optimised for scientific PDFs, multi-column layouts, and formula-heavy content. First run downloads ~5-8 GB of model weights into a persistent volume.
- **Auto** (recommended) — runs Docling first, scores the result against a configurable confidence threshold (default 0.95), and silently re-parses with MinerU when Docling underperforms. The chosen engine and confidence breakdown are persisted on `Source.metadata` and surfaced in the source detail view as a coloured badge.
- **Simple** — bypasses both parsers and uses lightweight text extraction. Fastest; loses table structure, headings, and bboxes.

MinerU is provided by an optional `services/mineru` container. When the container is unreachable the Settings page shows a red "MinerU offline" chip; auto-mode automatically degrades to docling-only with no user-visible failure. See [docs/troubleshooting/parser-engines.md](docs/troubleshooting/parser-engines.md) for service-down and threshold-tuning recipes.
```

## 8. The ARCHITECTURE.md update

Add a new subsection right after `### 4. AI Processing Layer` (line 115) or as part of `### 2. Domain Layer`. Exact paragraph + bullet block:

```markdown
### Source extraction pipeline

Document ingestion is dispatched through `apps/app-main/src/app_main/services/parsing/engine_dispatcher.py`, which maps the user's `parser_engine` setting (`simple` / `docling` / `mineru` / `auto`) and the file extension to a concrete extraction client. The two GPU-backed parser engines (`services/docling/`, `services/mineru/`) are independent FastAPI containers that share an input volume and expose a symmetric `/process` endpoint. Audio/video files bypass the dispatcher entirely and route to WhisperX via the in-process `IngestionWorkflow`.

When `parser_engine = "auto"`, the orchestrator `auto_fallback.py` runs Docling first, scores the result via `confidence.py::score_docling_extraction` (six weighted signals: OCR confidence, text density, heading rate, table success, image ratio, unknown-element ratio), and re-parses with MinerU only when the overall score is below `docling_min_confidence` (default 0.95). The chosen engine, confidence, signal breakdown, and a `fallback_triggered` flag are persisted on the per-source `Source.metadata` bag (SurrealDB `FLEXIBLE TYPE option<object>`, migration #43) and rendered in the UI by `ParserEngineBadge`.

- **Routing**: `apps/app-main/src/app_main/services/parsing/engine_dispatcher.py`
- **Auto-fallback**: `apps/app-main/src/app_main/services/parsing/auto_fallback.py`
- **Confidence**: `apps/app-main/src/app_main/services/parsing/confidence.py`
- **Provenance**: `Source.metadata` (declared in `migrations/43.surrealql`)
```

## 9. The troubleshooting page (`docs/troubleshooting/parser-engines.md`)

Minimum 3 scenarios required by the prompt. Recommended 4:

```markdown
# Parser engine troubleshooting

## MinerU container fails to start

**Symptom**: `docker compose up mineru` exits with `unable to find driver capabilities` or `nvidia-smi: command not found`.

**Cause**: MinerU's container image is CUDA 12.6-based and requires the NVIDIA Container Toolkit on the host.

**Fix**:
1. Install [NVIDIA Container Toolkit](https://docs.nvidia.com/datacenter/cloud-native/container-toolkit/latest/install-guide.html) on the host.
2. Verify with `docker run --rm --gpus all nvidia/cuda:12.6.3-runtime-ubuntu24.04 nvidia-smi`.
3. If you don't have an NVIDIA GPU at all, **do not start the MinerU service** — the main app boots fine without it (no `depends_on` link). Settings → Parser engine = `docling` will avoid every call site.

## Auto-fallback fires too often (confidence too aggressive)

**Symptom**: nearly every uploaded PDF shows the amber "MinerU (auto-fallback, conf …)" badge even when the docling output looks clean.

**Cause**: the default confidence threshold (0.95) is calibrated for academic / structured docs. Highly heterogeneous corpora (mixed Word→PDF exports, multi-language docs, image-rich marketing material) may legitimately score 0.85-0.94.

**Fix**:
1. Go to Settings → Content Processing → Parser engine.
2. Select **Auto** and lower the **Confidence threshold** slider to 0.85 (or run `apps/app-main/scripts/score_pdf_corpus.py` against your corpus and pick a percentile-based value; see [`docs/tracks/A-mineru/threshold-tuning.md`](../tracks/A-mineru/threshold-tuning.md)).
3. Reprocess the affected source(s) from the source-detail page.

## Rolling back to docling-only

**Symptom**: MinerU is causing more problems than it solves, or model weights are eating disk space.

**Fix**:
1. Settings → Parser engine → **Docling** (or **Simple** for the lightest path).
2. `docker compose stop mineru && docker compose rm mineru` to free the container.
3. (Optional) `docker volume rm open-notebook_mineru_models` to reclaim the 5-8 GB model cache. **Note**: this means re-downloading on the next start.
4. Per-source overrides are reset by reprocessing — the next `POST /sources/{id}/reprocess` without `parser_engine` in the body honours the new global setting.

## Bad confidence score on a document that looks fine

**Symptom**: a clean native PDF scores 0.78 and gets fallback'd; manually inspecting the docling output shows it's perfectly acceptable.

**Cause**: the score is signal-driven — most likely culprits: low heading count (`heading_rate` signal saturates at 2 headings/page; a doc with one heading per 5 pages scores 0.1 on this signal), or unusual element types showing up as `unknown`.

**Fix**:
1. Run `uv run --project apps/app-main python -m app_main.scripts.score_pdf_corpus path/to/file.pdf` to see the per-signal breakdown.
2. Decide if the low-scoring signal is meaningful for your corpus. If most docs lack headings legitimately, lower the threshold (see "Auto-fallback fires too often" above).
3. As a per-document override, reprocess with `parser_engine = 'docling'` to force the docling path even if auto would fall back.
```

## 10. Acceptance criteria (numbered, testable, matches prompt §"reviewer will check")

1. **`docs/tracks/A-mineru/threshold-tuning.md`** contains a Markdown table with rows for ≥ 3 real PDFs, each with: fixture name, docling overall score, decision-@-0.95, manual quality call.
2. **Default 0.95 confirmed OR new default proposed**. The report ends with one of the two bolded conclusion sentences from §6. If new default proposed, the PR diff includes the matching code change in `confidence.py` + `settings.py` + `SettingsForm.tsx`.
3. **`frontend/e2e/track-a/parser-engine-integration.spec.ts` exists** and passes locally via `npx playwright test e2e/track-a/parser-engine-integration.spec.ts`. Spec uses route mocks, no MinerU container required.
4. **`README.md` has a "Parser engines" subsection** with the exact copy block from §7, explaining all four engines.
5. **`docs/development/architecture.md` mentions the dispatcher + `Source.metadata`** via the paragraph block from §8 (≤ 2 paragraphs).
6. **`docs/troubleshooting/parser-engines.md` exists** with ≥ 3 troubleshooting scenarios from §9. `docs/troubleshooting/index.md` links to it.
7. **`docs/tracks/A-mineru/RETRO.md` exists** with three top-level sections (`## What worked`, `## What hurt`, `## Recommendations for tracks B-G`) — concrete bullet points, no marketing language.
8. **No regressions**:
   - `uv run --project apps/app-main pytest apps/app-main/tests/` — all previously-passing tests still pass; +1 new test for `score_pdf_corpus` helper.
   - `uv run --project packages/shared pytest packages/shared/tests/` — unchanged 105 passing.
   - `cd frontend && npm run lint && npx tsc --noEmit` — clean.
   - `cd frontend && npx playwright test e2e/track-a/` — 8 prior A.2 sub-tests + the new integration spec all pass.
9. **`docs/tracks/A-mineru/status.md`** has a final entry titled `## Phase A.3 — IMPLEMENTED` ending with **"Track A complete"**.
10. **`docs/FEATURE_ROADMAP.md`** Track A row marked done, with pointers to RETRO + threshold-tuning report.

## 11. Test strategy

- **Backend unit** (pytest): one new test `test_score_pdf_corpus.py` (smoke; the CLI's helper functions parse a synthetic `IngestionResult` into a Markdown row of the expected column count). Coverage target: ≥ 80% of `score_pdf_corpus.py` (it's small).
- **Frontend lint + types** (`npm run lint && npx tsc --noEmit`): zero new errors.
- **Playwright** (CI + local): existing 8 sub-tests + 1 new integration test = 9 sub-tests total. All route-mocked, all CI-runnable.
- **Manual smoke** (developer's machine, documented in the PR description):
  1. `docker compose up -d docling` then `cd apps/app-main && uv run python -m app_main.scripts.score_pdf_corpus ../../docling_input/` — confirm the printed Markdown table matches what was pasted into `threshold-tuning.md`.
  2. Visit Settings page, confirm threshold slider value matches whatever the report concluded (0.95 or the new default).
  3. Run `npx playwright test e2e/track-a/parser-engine-integration.spec.ts --headed` once locally — confirm the integration flow plays out visually.
- **Coverage of pre-existing functionality**: A.3 does NOT add new product modules to cover. The `confidence.py` / `auto_fallback.py` modules retain their A.1c coverage targets unchanged.

## 12. PR boundary + commit plan

**Branch**: `track/a-mineru-integration` (already created off `main`).

**Single PR** titled: `feat(track-a): A.3 integration + threshold tuning + docs (Track A complete)`.

**Suggested commit sequence** (each commit lands cleanly on its predecessor):

1. `chore(fixtures): add tiny synthetic PDFs + generator for parser-engine tests` — `apps/app-main/tests/fixtures/*.pdf`, `generate.py`, `README.md`.
2. `feat(app-main/scripts): score_pdf_corpus CLI for threshold tuning` — `apps/app-main/scripts/score_pdf_corpus.py`, `apps/app-main/tests/test_score_pdf_corpus.py`.
3. `docs(track-a): threshold tuning report + decision` — `docs/tracks/A-mineru/threshold-tuning.md`. If the report concludes "change default to X", **commit 4** below also rolls in the corresponding code change.
4. (conditional) `feat(parsing): bump default docling_min_confidence to X.XX per tuning` — `confidence.py` + `settings.py` + `SettingsForm.tsx`. Skip this commit if conclusion is "keep 0.95".
5. `test(e2e/track-a): integration spec covering full auto-fallback + reprocess override` — `frontend/e2e/track-a/parser-engine-integration.spec.ts`.
6. `docs: README parser engines + ARCHITECTURE source-extraction subsection` — `README.md`, `docs/development/architecture.md`.
7. `docs(troubleshooting): parser engines page + index link` — `docs/troubleshooting/parser-engines.md`, `docs/troubleshooting/index.md`.
8. `docs(track-a): RETRO + status.md final entry + FEATURE_ROADMAP done marker` — `docs/tracks/A-mineru/RETRO.md`, `docs/tracks/A-mineru/status.md`, `docs/FEATURE_ROADMAP.md`.

Order matters for one reason only: commits 1-2 must precede commit 3 (the report references the CLI by name) and commit 5 (the integration spec references the synthetic fixtures by path). Everything else is order-independent.

## 13. Risks + mitigations

| Risk | Mitigation |
|---|---|
| `reportlab` is not in the app-main deps, breaking `tests/fixtures/generate.py` | Verify upfront with `uv run --project apps/app-main python -c "import reportlab"`. If missing, either commit hand-crafted minimal PDFs as binary (no generator) and document the source, or add `reportlab` as a dev-only dep (low cost; ~3 MB). **Decision**: prefer no new dep — commit the three fixtures as raw bytes and skip `generate.py` if reportlab is unavailable. |
| Developer-local `docling_input/` PDFs differ between maintainers; tuning report not reproducible | Report explicitly names every fixture + lists fixture sizes; reviewer can re-run with their own corpus and append a second table. The decision sentence must hold across reasonable corpora, not just our sample. |
| Threshold change cascades into 3 files; risk of inconsistency | If commit 4 fires, run `rg "0\.95"` across `apps/app-main`, `packages/shared`, `frontend/src` after the change to ensure no straggler reference. Single grep, no script needed. |
| Playwright integration spec interacts with the live `next dev` build state (cached pages); flakes on CI | Spec opens new contexts per `test.beforeEach`, route-mocks before any navigation, asserts via `expect(...).toHaveText` with default Playwright timeout. Same pattern as the existing 4 A.2 specs (no flakes observed). |
| ARCHITECTURE.md is touched concurrently by future tracks | A.3 inserts in a stable, low-traffic section (after §4 AI Processing). Merge conflicts unlikely; if any, paragraph is self-contained and trivially re-applied. |

## 14. Open questions (escalate)

1. **ARCHITECTURE.md insertion point** — should the new "Source extraction pipeline" subsection live (a) after §4 AI Processing Layer (own subsection), or (b) inside §2 Domain Layer? **Default**: (a) — it's an infrastructure pipeline more than a domain concept. **Escalation**: confirm with reviewer.
2. **Where in README** — under `### Advanced Features` (line 316) or under `### 🛠️ Full Installation` (line 266)? **Default**: `### Advanced Features` — it's a feature, not a setup step. **Escalation**: confirm.
3. **Should the threshold-tuning report commit raw stdout from `score_pdf_corpus.py` as a code block, or just the curated Markdown table?** **Default**: curated table + a "Reproducibility" footnote with the exact command. Raw stdout is verbose and unstable across docling versions. **Escalation**: confirm.
4. **Should the integration Playwright spec also cover the graceful-degradation path (MinerU offline → auto-mode → docling-only succeeds)?** The parent plan A.3 task list mentions a separate `graceful-degradation.spec.ts`. **Default**: include graceful-degradation as a **second test block** inside `parser-engine-integration.spec.ts` (same file, separate `test('…')` call) — same mocks, just flip `/api/health/mineru` to `{healthy: false}`. Adds ~30 LOC. **Escalation**: if reviewer prefers two separate files, split.

## 15. Effort estimate

| Item | Effort |
|---|---|
| `score_pdf_corpus.py` CLI + smoke test | 0.15 day |
| Run tuning against local corpus + write `threshold-tuning.md` | 0.25 day |
| Synthetic fixture trio + `tests/fixtures/README.md` | 0.1 day |
| Playwright integration spec (incl. graceful-degradation sub-test) | 0.2 day |
| README + ARCHITECTURE.md + troubleshooting page | 0.15 day |
| RETRO.md + status.md final entry + FEATURE_ROADMAP marker | 0.1 day |
| Local QA / lint / tsc / PR write-up | 0.05 day |
| **Total** | **~1 day** |

Within the parent plan's A.3 budget (2-3 days). Underrunning because we already have all the backend + UI plumbing from A.1-A.2; A.3 is pure validation + docs. The 2-3-day budget assumed a 20-30 PDF labelled corpus (per parent plan); the prompt explicitly relaxes that to "3-5 PDFs" so the tuning step is much shorter.

## 16. Dependency map

```
A.2 (merged on main)
    ↓
A.3 (this PR — final phase of Track A)
    ↓
Track A complete; no follow-on PR within this track
```

Blocks: **nothing** within Track A.

Track-A-complete unblocks (for future tracks):
- Track B4 (confidence-scoring everywhere) — A.3's `score_pdf_corpus.py` is the reference implementation pattern for B4's per-entity scoring CLI.
- Track G (agent platform) — the `Source.metadata` provenance pattern (declared in migration #43, lifted from `ExtractionResult.metadata` in `SourceProcessor`) is the template for agent-run provenance bags.

## 17. Out of scope (explicit non-goals for A.3)

To keep the PR boundary tight, the following are **not** in A.3 — they belong to a future follow-up if/when they become real problems:

- Live SurrealDB schema round-trip test for `Source.metadata` (acknowledged gap from A.1c). Requires `pytest-docker` or testcontainers. Would land as `ci(testing): introduce surrealdb-testcontainer harness` after Track A.
- Real-MinerU CI smoke spec (would need GPU runners). Documented in `services/mineru/README.md` as a manual recipe.
- 20-30 PDF labelled corpus with fallback-precision metric (parent plan's original ambition). Relaxed to 3-5 fixtures per prompt; if the smaller corpus surfaces a need for a larger one, it lands as a follow-up data-collection task, not as A.3 work.
- Telemetry on fallback frequency (parent plan Risk 3 mitigation: "Track usage telemetry post-launch"). Belongs to whichever track introduces app-wide telemetry.
- "MinerU auto-fallback (auto-fallback)" badge copy revisions — current copy from A.2 is acceptable; UX polish belongs to a separate polish PR if needed.
